module.exports = {
  printerName: 'PRINTER_NAME',
  printerEnabled: true,
  imagePaths: {
    src: `${__dirname}/src`,
    dest: `${__dirname}/dest`,
    mask: `${__dirname}/mask.png`
  },
  imageWidth: 600,
  imageHeight: 400
};
